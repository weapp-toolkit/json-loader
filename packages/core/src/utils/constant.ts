/** app 模块的 chunk name */
export const APP_GROUP_NAME = 'app';

export const APP_PACKAGE_NAME = 'app';

/** 自定义 TabBar 的文件夹名称 */
export const CUSTOM_TAB_BAR_CONTEXT = 'custom-tab-bar';

/** 上层目录标识符 */
export const UPPER_DIR_SYMBOL_REG = /\.{2}/g;

/** 分包外部依赖拷贝目录的名称 */
export const PKG_OUTSIDE_DEP_DIRNAME = '_reference_';
/** 分包外部依赖表示上层目录的名称 */
export const PKG_OUTSIDE_DEP_UPPER_DIRNAME = '_upper_';

/** AssetsMap 默认的 ignore 配置 */
export const DEFAULT_ASSETS_MAP_IGNORES = [/.(js|ts)x?$/];
